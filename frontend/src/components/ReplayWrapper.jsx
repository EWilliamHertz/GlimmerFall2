import React, { useState, useEffect, useRef } from "react";
import { Play, Pause, SkipBack, SkipForward } from "lucide-react";
import GameBoard from "@/components/GameBoard"; // wait, GameBoard is in Arena.jsx!
